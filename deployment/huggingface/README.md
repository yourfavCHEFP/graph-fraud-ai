# Hugging Face Demo

This directory is the lightweight public-demo boundary for Graph Fraud AI.

For a hosted Space, configure `API_URL` to the deployed FastAPI `/predict` endpoint and avoid placing private model/data artifacts in the Space unless they are explicitly intended for public distribution.
